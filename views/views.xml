<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="ideastore.list">
      <field name="name">ideastore list</field>
      <field name="model">ideastore.ideastore</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="ideastore.action_window">
      <field name="name">ideastore window</field>
      <field name="res_model">ideastore.ideastore</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="ideastore.action_server">
      <field name="name">ideastore server</field>
      <field name="model_id" ref="model_ideastore_ideastore"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="ideastore" id="ideastore.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="ideastore.menu_1" parent="ideastore.menu_root"/>
    <menuitem name="Menu 2" id="ideastore.menu_2" parent="ideastore.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="ideastore.menu_1_list" parent="ideastore.menu_1"
              action="ideastore.action_window"/>
    <menuitem name="Server to list" id="ideastore" parent="ideastore.menu_2"
              action="ideastore.action_server"/>
    -->
  </data>
</openerp>